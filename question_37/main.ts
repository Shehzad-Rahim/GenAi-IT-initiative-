function shirt(size: string = "large" , message: string = "No Pain No Gain") {
    console.log(`'${size}' size with message '${message}' available in stock`);
};

shirt();
shirt("medium","Code is Love");
shirt("small","Hard times are temporary");
