let famousPerson : string = "Bill Gates";
let message : string = `${famousPerson} famous quote, “I choose a lazy person to do a hard job. Because a lazy person will find an easy way to do it.” `;
console.log(message);
